// 页面加载完成后执行
document.addEventListener('DOMContentLoaded', function() {
    // 初始化所有功能
    initDreamButton();
    initDrawingWall();
    initMessageWall();
    initStatistics();
    initParticleSystem(); // 初始化粒子系统
});